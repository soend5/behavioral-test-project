# 深度体验审计报告 v2.0
# 助教工作台 + Admin 管理后台 专项审计

> 审计日期：2026-01-09
> 审计角色：CXO + 企业级后台产品专家 + 金融投教业务顾问 + UX架构师 + 复杂系统重构顾问
> 审计方式：基于代码深度分析 + 角色行为模拟
> 审计范围：助教端（Coach Console）+ Admin 管理后台（Admin Console）

---

## 【最少补充清单】

在开始审计前，以下信息基于代码推断，若有实际截图/数据可进一步校准：
1. 实际用户量级（助教数、客户数、日活）
2. 助教实际使用频率和反馈
3. Admin 操作频率统计
4. 现有配置项的实际使用率

**但不影响本次审计，以下基于代码实现进行完整分析。**

---

## 【0】总体结论摘要（给 CEO 看）

### 助教端最大价值点
**「标签驱动型 SOP 实时陪跑提示」已形成闭环**——从测评结果自动生成标签，匹配 SOP 规则，输出可照读的一句话建议。这是核心差异化能力，但当前"最后一公里"（话术→复制→发送）的效率仍有提升空间。

### Admin 端最大结构问题
**「配置对象语义重叠 + 入口分散」是最大隐患**：
- SOP Definition / SOP Rule / 话术模板 / 训练计划 四套配置体系存在概念交叉
- 同一个"调整某阶段某画像用户的陪跑策略"的目标，需要在 3-4 个页面分别操作
- 未来维护者难以理解"改哪里影响哪里"

### 最危险的 3 个隐患

| 排序 | 隐患 | 风险等级 | 说明 |
|------|------|----------|------|
| 1 | **配置冲突无预警** | 🔴 高 | SOP规则、话术触发条件、训练计划可能产生逻辑冲突，系统无校验 |
| 2 | **助教端信息过载** | 🔴 高 | 客户详情页信息密度过高，关键决策信息被淹没 |
| 3 | **Admin 入口重复** | 🟡 中 | 同类配置多入口导致维护成本高、误操作风险大 |

### 最值得立刻做的 10 个优化点（按优先级）

| 优先级 | 优化点 | 影响 | 成本 |
|--------|--------|------|------|
| P0 | 助教端：客户详情页信息分层重构 | 决策效率+50% | 2天 |
| P0 | Admin：SOP+话术配置入口合并 | 减少配置冲突 | 3天 |
| P0 | 助教端：待办面板默认展开+优先级可视化 | 响应速度+30% | 1天 |
| P1 | Admin：配置影响预览功能 | 减少误操作 | 3天 |
| P1 | 助教端：话术面板置顶+快捷键复制 | 沟通效率+40% | 1天 |
| P1 | Admin：侧边栏重组（按职责而非对象） | 可理解性+50% | 2天 |
| P2 | 助教端：高风险用户红色标记 | 风险预警 | 1天 |
| P2 | Admin：配置变更审计增强 | 可追溯性 | 2天 |
| P2 | 助教端：跟进记录模板化 | 记录效率+30% | 1天 |
| P3 | Admin：批量导入导出 | 运营效率 | 3天 |

---

## 【1】多角色体验报告

### A. 新手助教（刚上岗 3 天，怕说错话）

**角色目标**：
- 快速上手，不犯错
- 找到"安全"的话术
- 理解系统给的建议是什么意思

**实际操作路径**：
```
1. 登录 /coach/login
2. 进入 /coach/dashboard 看到待办面板（折叠状态）
3. 点击展开待办，看到"新完成"客户
4. 点击进入客户详情 /coach/clients/[id]
5. 看到大量信息：参与者信息、最新测评、测评时间线、实时陪跑提示、标签...
6. 困惑：我该先看哪里？
7. 找到"实时陪跑提示区"，看到"可照读的一句话建议"
8. 但不确定这句话够不够用，想找更完整的话术
9. 发现右侧有"推荐话术"面板，但位置靠下，需要滚动
10. 复制话术，发送给客户
```

**情绪/压力变化**：
- 登录时：紧张（怕操作错误）
- 看到待办面板：稍安心（知道该做什么）
- 进入客户详情：困惑（信息太多，不知道重点）
- 找到话术：释然（有现成的可以用）
- 发送后：忐忑（不确定说得对不对）

**最大阻力点**：
1. **客户详情页信息层级不清**：新手不知道先看哪里
2. **话术面板位置靠下**：需要滚动才能看到
3. **合规提示过于醒目**：黄色警告框增加心理压力
4. **标签含义不直观**：`image:rule_executor` 需要翻译

**如果我是这个角色，最想要的 3 个功能**：
1. **"新手引导模式"**：首次进入客户详情时，高亮显示"先看这里"
2. **话术置顶**：把推荐话术放在最显眼的位置
3. **一键复制+发送记录**：复制话术后自动记录为跟进日志

---

### B. 成熟助教（追求转化效率和稳定交付）

**角色目标**：
- 快速处理待办，不漏单
- 高效沟通，减少重复劳动
- 追踪转化进度

**实际操作路径**：
```
1. 登录后直接看待办面板
2. 按优先级处理：新完成 > 即将过期 > 进行中
3. 批量打开多个客户详情（多标签页）
4. 快速扫描：画像类型 + 阶段 + 核心建议
5. 复制话术，微调后发送
6. 记录跟进日志，设置下次跟进时间
7. 回到待办，处理下一个
```

**情绪/压力变化**：
- 登录时：高效模式启动
- 待办面板：满意（清晰的优先级）
- 批量处理：略烦躁（每个页面都要滚动找话术）
- 记录跟进：还行（但希望更快）
- 一天结束：疲惫（重复操作太多）

**最大阻力点**：
1. **无批量操作**：不能批量标记、批量发送
2. **话术需要手动微调**：变量替换不够智能
3. **跟进记录无模板**：每次都要手打
4. **无转化漏斗视图**：不知道整体转化情况

**如果我是这个角色，最想要的 3 个功能**：
1. **批量操作**：批量标记已处理、批量创建邀请
2. **话术智能填充**：自动填入客户名、画像特点
3. **个人转化看板**：我的客户转化漏斗

---

### C. 销售型助教（强目标、强转化）

**角色目标**：
- 最大化转化率
- 识别高价值客户
- 快速推进付费

**实际操作路径**：
```
1. 登录后想看"高潜力客户"列表
2. 发现客户列表无筛选功能
3. 只能逐个点击查看
4. 进入客户详情，寻找"价值信号"
5. 看到分层标签（high_potential/needs_attention）但不够醒目
6. 想找"转化话术"，发现话术分类有"转化"类别
7. 复制话术，推进转化
8. 想记录"意向度"，发现只能用 coach:* 标签
```

**情绪/压力变化**：
- 登录时：目标导向
- 找不到筛选：沮丧
- 逐个查看：低效感
- 找到转化话术：稍好
- 无法快速标记意向：烦躁

**最大阻力点**：
1. **无客户筛选**：不能按分层、按画像筛选
2. **分层标签不够醒目**：高潜力客户没有视觉突出
3. **无意向度字段**：只能用标签模拟
4. **无转化阶段追踪**：不知道客户在哪个转化节点

**如果我是这个角色，最想要的 3 个功能**：
1. **客户筛选器**：按分层、画像、最近活动筛选
2. **意向度评分**：1-5星快速标记
3. **转化阶段看板**：看板视图展示转化进度

---

### D. 风控/合规敏感助教（最怕踩线和纠纷）

**角色目标**：
- 绝对不说违规的话
- 有据可查，可追溯
- 遇到风险用户能识别

**实际操作路径**：
```
1. 登录后关注"需关注"类型客户
2. 进入客户详情，首先看合规提示
3. 看到黄色警告框："本测评及结果用于生成行为结构画像..."
4. 查看"禁用行为"列表
5. 选择话术时，担心话术是否合规
6. 发送前反复检查
7. 详细记录跟进内容，以备查证
```

**情绪/压力变化**：
- 登录时：谨慎
- 看到合规提示：安心（系统有提醒）
- 看禁用行为：认真记住
- 选话术：犹豫（不确定是否合规）
- 发送后：记录详细日志

**最大阻力点**：
1. **话术无合规标记**：不知道哪些话术经过合规审核
2. **高风险用户不够醒目**：`needs_attention` 标签不够突出
3. **禁用行为位置靠后**：需要滚动才能看到
4. **无合规检查工具**：发送前无法校验

**如果我是这个角色，最想要的 3 个功能**：
1. **话术合规标记**：标注"已审核"/"需谨慎"
2. **高风险用户红色警告**：进入详情页时弹出提醒
3. **禁用行为置顶**：在话术面板上方显示

---

### E. Admin-运营负责人（关注效率、规模、ROI）

**角色目标**：
- 监控整体运营数据
- 优化转化漏斗
- 管理助教团队效能

**实际操作路径**：
```
1. 登录 /admin/login
2. 进入 /admin（总览页）
3. 看到内容资产状态，但没有运营数据
4. 点击进入 /admin/dashboard（数据看板）
5. 看到核心指标：今日测评、完成率、联系率
6. 看到转化漏斗、画像分布、助教排行
7. 想调整某个阶段的策略
8. 不确定该去 SOP配置 还是 话术库
9. 先去 SOP配置，发现是规则配置
10. 再去话术库，发现是内容配置
11. 困惑：这两个有什么关系？
```

**情绪/压力变化**：
- 登录时：目标明确
- 看到数据看板：满意
- 想调整策略：困惑（入口太多）
- 在多个页面切换：烦躁
- 不确定改对没有：焦虑

**最大阻力点**：
1. **总览页无运营数据**：需要额外点击进入数据看板
2. **SOP 和话术关系不清**：不知道改哪个影响什么
3. **无配置预览**：改完不知道效果
4. **无配置版本管理**：改错了无法回滚

**如果我是这个角色，最想要的 3 个功能**：
1. **总览页集成核心指标**：不用额外点击
2. **策略配置中心**：统一管理 SOP+话术+训练
3. **配置预览**：改完能看到"这会影响哪些客户"

---

### F. Admin-产品/数据负责人（关注系统结构与可扩展性）

**角色目标**：
- 理解系统数据流
- 评估扩展性
- 规划迭代路径

**实际操作路径**：
```
1. 登录后查看各模块
2. 题库 → 题目 → 选项：三级结构清晰
3. SOP配置 → Definition + Rule：两级结构
4. 话术库：独立模块，有触发条件
5. 训练计划 → 任务：两级结构
6. 发现：SOP Rule 的 requiredTags 和话术的 triggerTags 是同一套标签体系
7. 但配置入口完全分离
8. 担心：未来标签体系变更时，需要同步修改多处
```

**情绪/压力变化**：
- 登录时：分析模式
- 看到结构：基本清晰
- 发现重叠：担忧
- 评估扩展性：有顾虑

**最大阻力点**：
1. **标签体系分散**：SOP、话术、分层都用标签，但无统一管理
2. **配置依赖不透明**：改了标签不知道影响哪些规则
3. **无配置导出**：无法备份或迁移
4. **无配置校验**：可能配置出冲突规则

**如果我是这个角色，最想要的 3 个功能**：
1. **标签管理中心**：统一管理所有标签定义
2. **配置依赖图**：可视化展示配置间关系
3. **配置导入导出**：支持 JSON/CSV 备份

---

### G. 技术/架构视角（关注重复系统/重复CRUD/重复配置）

**角色目标**：
- 识别技术债务
- 评估代码可维护性
- 规划重构路径

**实际操作路径（代码审查）**：
```
1. 查看 prisma/schema.prisma
2. 发现多个"配置类"模型：
   - SopDefinition + SopRule（SOP配置）
   - ScriptTemplate（话术配置）
   - TrainingPlan + TrainingTask（训练配置）
   - CoachingStage（阶段配置）
3. 这些模型都有类似字段：
   - status (active/inactive)
   - triggerStage / sopStage / stage
   - triggerTags / requiredTags / excludedTags
4. 对应的 API 路由：
   - /api/admin/sop/definition + /api/admin/sop/rule
   - /api/admin/scripts
   - /api/admin/training/plans + /api/admin/training/tasks
5. 对应的前端页面：
   - /admin/sop
   - /admin/scripts
   - /admin/training
6. 发现：三套独立的 CRUD 实现，逻辑相似但分散
```

**最大阻力点**：
1. **重复 CRUD 模式**：每个配置模块都有相似的列表/创建/编辑/删除逻辑
2. **标签字段命名不一致**：`triggerTags` vs `requiredTags` vs `tags`
3. **状态字段重复**：每个模型都有 `status`，但无统一枚举
4. **无配置基类**：无法复用配置管理逻辑

**技术债务清单**：

| 债务类型 | 具体问题 | 影响 | 建议 |
|----------|----------|------|------|
| 重复代码 | 3套配置CRUD逻辑相似 | 维护成本高 | 抽象配置管理基类 |
| 命名不一致 | 标签字段名不统一 | 理解成本高 | 统一为 `tags` |
| 缺少枚举 | status 字段无类型约束 | 可能出错 | 定义 ConfigStatus 枚举 |
| 无版本控制 | 配置变更无历史记录 | 无法回滚 | 增加配置版本表 |

---

### H. 未来维护者视角（6个月后接手后台的人）

**角色目标**：
- 快速理解系统
- 能安全地做修改
- 知道"改哪里影响哪里"

**实际操作路径（接手流程）**：
```
1. 阅读 README.md（假设有）
2. 查看数据库结构 prisma/schema.prisma
3. 看到 20+ 个模型，需要理解关系
4. 查看 Admin 导航：
   - 总览、系统设置、题库、SOP配置、画像文案、内训手册、方法论、助教账号、审计日志
5. 还有隐藏入口：
   - /admin/dashboard（数据看板）
   - /admin/scripts（话术库）
   - /admin/training（训练计划）
6. 困惑：为什么有些在导航里，有些不在？
7. 想修改"某阶段的推荐策略"
8. 不确定该改 SOP Definition 还是 SOP Rule 还是话术
9. 查看代码，发现：
   - SOP 影响 realtime_panel
   - 话术影响 ScriptPanel
   - 两者都基于 stage + tags 匹配
10. 担心：改了一个会不会影响另一个？
```

**最大阻力点**：
1. **导航不完整**：部分页面不在导航中
2. **模块关系不清**：SOP、话术、训练的关系需要看代码才能理解
3. **无架构文档**：没有系统架构图
4. **影响范围不透明**：改配置不知道影响哪些功能

**如果我是这个角色，最想要的 3 个功能**：
1. **完整的导航**：所有管理页面都在导航中
2. **架构文档**：说明各模块关系
3. **配置影响说明**：每个配置项说明"这会影响什么"

---

## 【2】助教端功能规划建议

### 必须补齐的核心功能（否则无法规模化）

| 功能 | 当前状态 | 规模化风险 | 建议优先级 |
|------|----------|------------|------------|
| 客户筛选器 | ❌ 缺失 | 客户多了找不到重点 | P0 |
| 批量操作 | ❌ 缺失 | 效率瓶颈 | P1 |
| 跟进模板 | ❌ 缺失 | 记录质量参差 | P1 |
| 个人看板 | ❌ 缺失 | 无法自我评估 | P2 |
| 高风险预警 | ⚠️ 弱 | 风险客户被忽略 | P0 |

### 可配置 vs 不可配置的边界

| 功能 | 建议 | 理由 |
|------|------|------|
| 话术内容 | ✅ 可配置 | 运营需要灵活调整 |
| 话术触发条件 | ✅ 可配置 | 策略需要迭代 |
| 分层规则 | ✅ 可配置 | 业务逻辑可能变化 |
| 待办优先级规则 | ⚠️ 部分可配置 | 核心逻辑固定，权重可调 |
| 标签显示映射 | ✅ 可配置 | 文案需要优化 |
| 页面布局 | ❌ 不可配置 | 保持一致性 |

### 哪些功能应该"系统先说话，助教只补充"

| 场景 | 系统应该做 | 助教补充 |
|------|------------|----------|
| 首次沟通 | 自动推荐话术+填充变量 | 微调措辞 |
| 风险识别 | 自动标记+弹出提醒 | 确认处理 |
| 跟进提醒 | 自动计算下次跟进时间 | 调整日期 |
| 复测建议 | 自动判断是否该复测 | 决定是否执行 |
| 分层标签 | 自动计算分层 | 手动调整 |

### 哪些信息应该从助教端移除/折叠

| 信息 | 当前状态 | 建议 | 理由 |
|------|----------|------|------|
| 客户ID | 显示 | 折叠到"更多" | 助教不需要 |
| 原始标签（如 `image:rule_executor`） | 显示 | 只显示中文映射 | 避免技术术语 |
| 测评时间线详情 | 展开 | 默认折叠 | 非高频查看 |
| 逐题答案 | 展开 | 默认折叠 | 信息过载 |
| 合规提示 | 黄色醒目框 | 改为灰色小字 | 减少心理压力 |

---

## 【3】Admin 端功能与结构优化（本次重点）

### A. 信息架构重排建议

**当前导航结构**：
```
管理后台
├── 总览（内容资产状态）
├── 系统设置
├── 题库
├── SOP 配置
├── 画像文案
├── 内训手册
├── 方法论
├── 助教账号
└── 审计日志

隐藏页面（不在导航中）：
├── /admin/dashboard（数据看板）
├── /admin/scripts（话术库）
├── /admin/training（训练计划）
├── /admin/questions（题目管理）
├── /admin/options（选项管理）
```

**问题分析**：
1. 导航按"对象类型"组织，而非"职责/任务"
2. 重要页面（数据看板、话术库）不在导航中
3. 内容管理（题库/画像/内训/方法论）和策略管理（SOP/话术）混在一起

**建议重排**：
```
管理后台
├── 📊 数据看板（首页）
│   └── 核心指标 + 漏斗 + 助教排行
│
├── 🎯 策略中心（合并）
│   ├── SOP 规则
│   ├── 话术库
│   └── 训练计划
│
├── 📚 内容管理
│   ├── 题库（含题目/选项）
│   ├── 画像文案
│   ├── 内训手册
│   └── 方法论
│
├── 👥 团队管理
│   ├── 助教账号
│   └── 权限配置（未来）
│
├── ⚙️ 系统设置
│   ├── 默认配置
│   └── 标签管理（新增）
│
└── 📋 审计日志
```

### B. 权限与角色设计建议

**当前权限模型**：
- `admin`：全部权限
- `coach`：仅自己的客户/邀请

**建议扩展**：

| 角色 | 权限范围 | 适用场景 |
|------|----------|----------|
| super_admin | 全部 | 系统管理员 |
| content_admin | 内容管理（题库/画像/内训/方法论） | 内容运营 |
| strategy_admin | 策略管理（SOP/话术/训练） | 策略运营 |
| coach_manager | 查看所有助教数据 + 分配客户 | 助教主管 |
| coach | 仅自己的客户 | 普通助教 |

**权限矩阵**：

| 功能 | super_admin | content_admin | strategy_admin | coach_manager | coach |
|------|-------------|---------------|----------------|---------------|-------|
| 数据看板 | ✅ | ✅ | ✅ | ✅ | ❌ |
| 题库管理 | ✅ | ✅ | ❌ | ❌ | ❌ |
| SOP配置 | ✅ | ❌ | ✅ | ❌ | ❌ |
| 话术管理 | ✅ | ❌ | ✅ | ❌ | ❌ |
| 助教管理 | ✅ | ❌ | ❌ | ✅ | ❌ |
| 审计日志 | ✅ | ✅ | ✅ | ✅ | ❌ |

### C. 哪些能力必须产品化，不能靠人管

| 能力 | 当前状态 | 风险 | 产品化建议 |
|------|----------|------|------------|
| 配置冲突检测 | ❌ 无 | 配置矛盾导致异常 | 保存时自动校验 |
| 配置影响预览 | ❌ 无 | 改错不知道 | 显示"影响 N 个客户" |
| 配置版本管理 | ❌ 无 | 无法回滚 | 自动保存历史版本 |
| 标签一致性 | ❌ 无 | 标签拼写错误 | 标签选择器（非自由输入） |
| 话术合规审核 | ❌ 无 | 违规话术上线 | 审核流程 |

### D. 按钮/菜单合理性审计

**不合理入口清单**：

| 入口 | 位置 | 问题 | 建议 |
|------|------|------|------|
| "题目管理"按钮 | 题库列表每行 | 应该是子页面，不是独立入口 | 改为展开/折叠 |
| /admin/dashboard | 不在导航 | 重要页面被隐藏 | 加入导航，作为首页 |
| /admin/scripts | 不在导航 | 重要页面被隐藏 | 加入导航 |
| /admin/training | 不在导航 | 重要页面被隐藏 | 加入导航 |
| "SOP 定义"和"匹配规则"Tab | SOP页面 | 概念分离增加理解成本 | 合并为树形结构 |
| "新建 SOP"和"新建规则" | SOP页面 | 两个创建入口 | 统一为"新建策略" |

**具体建议**：

| 入口 | 动作 | 理由 |
|------|------|------|
| /admin/dashboard | 移动到导航首位 | 最常用 |
| /admin/scripts | 加入导航"策略中心" | 核心功能 |
| /admin/training | 加入导航"策略中心" | 核心功能 |
| "题目管理"按钮 | 改为行内展开 | 减少页面跳转 |
| SOP Definition/Rule Tab | 合并为树形 | 减少概念 |
| "总览"页面 | 重命名为"内容状态" | 避免与数据看板混淆 |

### E. 重叠矩阵（必须用表格）

| 功能名称 | 当前入口 | 实际职责 | 是否重叠 | 重叠原因 | 建议动作 | 预期收益 |
|----------|----------|----------|----------|----------|----------|----------|
| SOP Definition | Admin /admin/sop | 定义陪跑策略内容 | 部分 | 与话术的"策略建议"功能重叠 | 合并到"策略中心" | 减少配置入口 |
| SOP Rule | Admin /admin/sop | 定义匹配条件 | 部分 | 与话术的 triggerTags 功能重叠 | 统一触发条件配置 | 减少重复配置 |
| 话术模板 | Admin /admin/scripts | 定义沟通话术 | 部分 | 与 SOP strategyList 功能重叠 | 话术作为 SOP 的"执行层" | 职责清晰 |
| 话术触发条件 | Admin /admin/scripts | 定义话术匹配规则 | 是 | 与 SOP Rule 完全重叠 | 复用 SOP Rule 的匹配逻辑 | 减少重复开发 |
| 训练计划 | Admin /admin/training | 定义训练内容 | 否 | 独立功能 | 保留，加入导航 | - |
| 训练任务 | Admin /admin/training | 定义每日任务 | 否 | 独立功能 | 保留 | - |
| 客户分层 | 代码 lib/customer-segment.ts | 自动计算分层 | 部分 | 与 coach:* 标签功能重叠 | 分层规则可配置化 | 运营灵活性 |
| 复测提醒 | 代码 lib/retest-reminder.ts | 计算复测时机 | 否 | 独立功能 | 规则可配置化 | 运营灵活性 |
| 标签显示映射 | 代码 lib/tag-display.ts | 标签中文映射 | 否 | 独立功能 | 移到 Admin 可配置 | 运营灵活性 |
| 阶段定义 | DB CoachingStage | 定义陪跑阶段 | 部分 | 与 SOP sopStage 重叠 | 统一阶段管理 | 减少概念 |
| 画像文案 | Admin /admin/archetypes | 定义画像描述 | 否 | 独立功能 | 保留 | - |

**重叠关系图**：

```
┌─────────────────────────────────────────────────────────────────┐
│                        策略配置层                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐         │
│  │ SOP         │    │ 话术库      │    │ 训练计划    │         │
│  │ Definition  │    │ Script      │    │ Training    │         │
│  │             │    │ Template    │    │ Plan        │         │
│  └──────┬──────┘    └──────┬──────┘    └─────────────┘         │
│         │                  │                                    │
│         │  ┌───────────────┴───────────────┐                   │
│         │  │ 重叠区域：触发条件配置          │                   │
│         │  │ - SOP Rule (requiredTags)     │                   │
│         │  │ - Script (triggerTags)        │                   │
│         │  └───────────────────────────────┘                   │
│         │                                                       │
│  ┌──────┴──────┐                                               │
│  │ SOP Rule    │ ←── 应该成为统一的"匹配引擎"                   │
│  └─────────────┘                                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                        执行层（助教端）                          │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐         │
│  │ realtime    │    │ Script      │    │ Training    │         │
│  │ Panel       │    │ Panel       │    │ Page        │         │
│  │ (SOP输出)   │    │ (话术输出)  │    │ (训练输出)  │         │
│  └─────────────┘    └─────────────┘    └─────────────┘         │
└─────────────────────────────────────────────────────────────────┘
```

**重构建议**：

1. **统一匹配引擎**：SOP Rule 作为唯一的匹配规则配置入口
2. **话术关联 SOP**：话术模板关联到 SOP Definition，而非独立配置触发条件
3. **策略中心**：合并 SOP + 话术 + 训练的配置入口

---

## 【4】UI/UX 改进建议

### 助教端改进

**信息结构改进**：

| 区域 | 当前问题 | 改进方案 |
|------|----------|----------|
| 客户详情页首屏 | 信息平铺，无重点 | 关键信息卡片置顶（画像+阶段+分层） |
| 话术面板 | 位置靠下 | 移到右侧顶部 |
| 实时陪跑提示 | 与话术分离 | 合并为"沟通建议"区块 |
| 测评详情 | 默认展开 | 默认折叠 |
| 标签显示 | 显示原始标签 | 只显示中文映射 |

**交互改进**：

| 交互 | 当前问题 | 改进方案 |
|------|----------|----------|
| 话术复制 | 点击"复制"按钮 | 增加快捷键 Ctrl+C |
| 跟进记录 | 每次手打 | 提供模板选择 |
| 待办处理 | 逐个点击 | 支持键盘导航 |
| 客户切换 | 返回列表再点击 | 增加上一个/下一个 |

**文案改进**：

| 位置 | 改前 | 改后 |
|------|------|------|
| 合规提示 | "本测评及结果用于生成行为结构画像与沟通建议参考，不构成投资顾问服务或任何买卖建议，不承诺收益。" | "以下为沟通参考，请勿作为投资建议" |
| 待办面板标题 | "待处理事项" | "今日待办" |
| 话术面板标题 | "推荐话术" | "💬 一键话术" |
| 跟进记录标题 | "跟进记录" | "📝 沟通记录" |

### Admin 端改进

**信息结构改进**：

| 区域 | 当前问题 | 改进方案 |
|------|----------|----------|
| 导航 | 按对象组织 | 按职责组织 |
| 总览页 | 只有内容状态 | 集成核心指标 |
| SOP页面 | Definition/Rule 分Tab | 树形结构展示 |
| 话术页面 | 独立页面 | 集成到策略中心 |

**文案改进**：

| 位置 | 改前 | 改后 |
|------|------|------|
| 导航"总览" | "总览" | "内容状态" |
| SOP页面标题 | "SOP 配置管理" | "陪跑策略配置" |
| SOP Definition | "SOP 定义" | "策略内容" |
| SOP Rule | "匹配规则" | "触发条件" |
| 话术页面标题 | "话术库管理" | "沟通话术" |
| triggerTags | "触发标签" | "适用场景" |

---

## 【5】系统级升级建议

### 数据与埋点

**当前已有**：
- TrackingEvent 表（埋点事件）
- DailyStats 表（每日统计快照）
- 基础埋点：landing_page_view, quiz_start, quiz_submit, result_contact_click 等

**缺失的关键指标**：

| 指标 | 用途 | 建议埋点 |
|------|------|----------|
| 配置变更频率 | 判断配置是否稳定 | admin_config_update |
| 配置冲突次数 | 判断重叠是否解决 | config_conflict_detected |
| 话术使用后转化 | 评估话术效果 | script_use_conversion |
| 助教响应时长 | 评估效率 | coach_response_time |
| 客户分层准确率 | 评估分层模型 | segment_accuracy |

**建议新增埋点**：

```typescript
// 配置相关
ADMIN_SOP_CREATE: 'admin_sop_create',
ADMIN_SOP_UPDATE: 'admin_sop_update',
ADMIN_SCRIPT_CREATE: 'admin_script_create',
ADMIN_SCRIPT_UPDATE: 'admin_script_update',
CONFIG_CONFLICT_DETECTED: 'config_conflict_detected',

// 助教效率相关
COACH_TODO_RESPONSE_TIME: 'coach_todo_response_time',
COACH_SCRIPT_CONVERSION: 'coach_script_conversion',
COACH_FOLLOWUP_QUALITY: 'coach_followup_quality',
```

### 风险与合规模型

**应系统化的提醒**：

| 场景 | 当前状态 | 建议 |
|------|----------|------|
| 高风险用户进入 | 标签显示 | 弹窗提醒 |
| 话术包含敏感词 | 无检测 | 保存时校验 |
| 助教长时间未跟进 | 无提醒 | 自动提醒 |
| 客户投诉 | 无记录 | 增加投诉记录功能 |

**应模板化的话术**：

| 场景 | 建议模板 |
|------|----------|
| 首次沟通 | 6画像 × 3阶段 = 18套 |
| 风险提示 | 通用1套 + 高风险1套 |
| 复测邀请 | 训练完成1套 + 定期1套 |
| 转化推进 | 3阶段各1套 |

### 自动化/智能化路径

**规则引擎驱动**：

| 功能 | 当前 | 建议 |
|------|------|------|
| SOP 匹配 | ✅ 已实现 | 增加匹配日志 |
| 话术推荐 | ✅ 已实现 | 增加效果反馈 |
| 分层计算 | ✅ 已实现 | 规则可配置化 |
| 复测提醒 | ✅ 已实现 | 规则可配置化 |
| 待办优先级 | ✅ 已实现 | 权重可配置化 |

**内容驱动**：

| 功能 | 当前 | 建议 |
|------|------|------|
| 话术变量替换 | ✅ 已实现 | 增加更多变量 |
| 画像描述 | ✅ 已实现 | 支持多版本 |
| 训练内容 | ✅ 已实现 | 支持个性化 |

---

## 【6】路线图建议

### Quick Wins（1-2 周）

**目标**：解决"入口重复/命名冲突/可理解性"类问题

| 任务 | 工时 | 验收指标 |
|------|------|----------|
| Admin 导航重组 | 1天 | 所有页面可从导航访问 |
| 数据看板设为首页 | 0.5天 | 登录后直达看板 |
| SOP/话术页面文案优化 | 0.5天 | 用户理解成本降低 |
| 助教端话术面板置顶 | 0.5天 | 话术使用率+20% |
| 待办面板默认展开 | 0.5天 | 响应时间-15% |
| 合规提示措辞优化 | 0.5天 | 助教压力感降低 |
| 客户详情页信息分层 | 1天 | 关键信息可见性+50% |

**验收指标**：
- Admin 导航完整度：100%（所有页面可达）
- 助教话术使用率：+20%
- 助教待办响应时间：-15%
- 配置理解成本：用户调研评分提升

### 30 天版本

**目标**：完成模块合并与职责边界重划

| 任务 | 工时 | 验收指标 |
|------|------|----------|
| 策略中心页面开发 | 3天 | SOP+话术统一入口 |
| 话术关联 SOP 重构 | 2天 | 消除触发条件重复配置 |
| 配置影响预览功能 | 2天 | 配置前可见影响范围 |
| 标签管理中心 | 2天 | 标签统一管理 |
| 客户筛选器 | 2天 | 支持分层/画像筛选 |
| 高风险用户预警 | 1天 | 红色标记+弹窗 |
| 跟进记录模板 | 1天 | 模板选择功能 |

**验收指标**：
- 配置入口数量：从 4 个减少到 2 个
- 配置冲突率：-80%
- 助教筛选效率：+50%
- 高风险用户响应率：+30%

### 90 天版本

**目标**：建立可维护的"配置层/执行层/监控层"分层架构

| 任务 | 工时 | 验收指标 |
|------|------|----------|
| 配置版本管理 | 3天 | 支持回滚 |
| 配置导入导出 | 2天 | JSON/CSV 支持 |
| 配置依赖图可视化 | 3天 | 可视化展示关系 |
| 权限体系扩展 | 3天 | 5级权限 |
| 批量操作功能 | 2天 | 批量标记/发送 |
| 个人转化看板 | 2天 | 助教自我评估 |
| 配置审核流程 | 3天 | 话术上线需审核 |

**验收指标**：
- 配置回滚成功率：100%
- 新人上手时间：从 3 天降到 1 天
- 配置误操作率：-90%
- 助教效率：+40%

---

## 【7】评分与警示

### 综合评分

| 维度 | 评分 | 扣分原因 |
|------|------|----------|
| **助教可用性** | 6.5/10 | 信息过载（-2）、话术位置不佳（-1）、无筛选（-0.5） |
| **Admin 可控性** | 5.5/10 | 入口重复（-2）、配置冲突无预警（-1.5）、导航不完整（-1） |
| **决策支持强度** | 7/10 | SOP 匹配已实现（+2）、话术推荐已实现（+1）、但缺乏效果反馈（-1） |
| **规模化潜力** | 5/10 | 无批量操作（-2）、无客户筛选（-1.5）、配置维护成本高（-1.5） |
| **合规与风险控制成熟度** | 6/10 | 合规提示已有（+1）、禁用行为已有（+1）、但高风险预警不足（-2） |
| **结构清晰度与可维护性** | 4.5/10 | 配置重叠（-2.5）、导航不完整（-1.5）、无架构文档（-1.5） |

### 总分：5.75/10

### 关键警示

🔴 **高风险**：
1. 配置冲突无检测——可能导致策略矛盾，助教收到错误建议
2. 高风险用户无醒目预警——可能导致合规风险
3. 配置无版本管理——改错无法回滚

🟡 **中风险**：
1. 导航不完整——新人找不到功能
2. 话术与 SOP 触发条件重复——维护成本高
3. 无批量操作——规模化瓶颈

🟢 **已做好**：
1. SOP 匹配引擎——核心能力已实现
2. 话术推荐——智能匹配已实现
3. 审计日志——操作可追溯
4. CSRF 保护——安全已覆盖
5. 分层计算——自动化已实现

---

## 【附录】关键任务路径犹豫点记录

### 任务：调整"某阶段 + 某画像用户"的陪跑策略

**犹豫点**：

1. **入口选择犹豫**：
   - 进入 /admin/sop → 看到 SOP Definition 和 Rule 两个 Tab
   - 进入 /admin/scripts → 看到话术列表
   - 不确定：改 SOP 还是改话术？

2. **概念理解犹豫**：
   - SOP Definition 的 `strategyList` 是"推荐策略"
   - 话术的 `content` 是"具体话术"
   - 两者什么关系？

3. **配置影响犹豫**：
   - 改了 SOP Rule 的 `requiredTags`，会影响哪些客户？
   - 改了话术的 `triggerTags`，会影响哪些客户？
   - 两者会冲突吗？

**犹豫来源**：
- 页面：/admin/sop 和 /admin/scripts 是独立页面
- 按钮：两个页面都有"新建"按钮
- 字段：`requiredTags` 和 `triggerTags` 命名不同但功能相似

### 任务：让"助教端看到的建议"更精确

**犹豫点**：

1. **realtime_panel 来源**：
   - 代码显示来自 SOP 匹配结果
   - 但话术面板是独立匹配

2. **配置入口**：
   - 改 realtime_panel 内容 → 去 /admin/sop 改 SOP Definition
   - 改话术推荐 → 去 /admin/scripts 改话术

3. **语义冲突**：
   - SOP 的 `coreGoal` 和话术的 `content` 可能不一致
   - 助教看到两个不同的建议

### 任务：做一次版本迭代（优化结果页→联系助教转化）

**犹豫点**：

1. **产品页面 vs 后台配置**：
   - 结果页 UI 改动 → 产品/前端
   - 画像文案改动 → /admin/archetypes
   - 助教信息展示 → 产品/前端

2. **不该重复做的**：
   - 画像描述在 Archetype 表
   - 画像描述也在结果页代码中有硬编码
   - 应该统一从 Archetype 读取

---

## 【附录】代码级发现

### 重复配置字段

```typescript
// SOP Rule
requiredTags: string[]    // JSON array
excludedTags: string[]    // JSON array

// Script Template  
triggerTags: string[]     // JSON array

// 两者功能相同，但命名不同
```

### 重复匹配逻辑

```typescript
// lib/sop-matcher.ts - SOP 匹配
const hasAllRequired = requiredTags.every(tag => tags.includes(tag));
const hasNoExcluded = !excludedTags.some(tag => tags.includes(tag));

// app/api/coach/scripts/route.ts - 话术匹配
// 类似的匹配逻辑，但独立实现
```

### 建议重构方向

```typescript
// 统一匹配引擎
interface MatchRule {
  requiredTags: string[];
  excludedTags: string[];
  requiredStage?: string;
  confidence: number;
}

// SOP 和话术都使用同一套匹配规则
interface SopDefinition {
  rules: MatchRule[];
  scripts: ScriptTemplate[];  // 话术作为 SOP 的子对象
}
```

---

*报告完成时间：2026-01-09*
*报告版本：v2.0*
*审计方式：代码深度分析 + 角色行为模拟*
